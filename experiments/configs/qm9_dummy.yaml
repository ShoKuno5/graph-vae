# tiny config: GPU1 / epoch1 / small batch
trainer:
  seed: 42
  epochs: 1
  batch_size: 32
model:
  hidden_dim: 64
  latent_dim: 32
data:
  name: QM9
  root: /dataset/QM9          # ジョブスクリプト側で /dataset にバインドする
log_dir: /workspace/runs      # 同様に /workspace/runs をバインド
